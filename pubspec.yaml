name: bubble_pop_adventure
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flame: ^1.22.0
  flame_forge2d: ^0.18.2+3
  flutter:
    sdk: flutter
  get: ^4.6.6
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
